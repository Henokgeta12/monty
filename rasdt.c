#include "monty.h"

/**
 * rasdt- The opcode nop doesn’t do anything.
 * @stack:ouble pointer to the stack.
 * @line_number: line number.
 */
void rasdt(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}
